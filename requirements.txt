# StarlyProxy Requirements
# Python dependencies for multi-instance proxy management

# Core dependencies (required)
netifaces>=0.11.0
psutil>=5.9.0
pyyaml>=6.0

# Web panel (required)
flask>=2.3.0
flask-cors>=4.0.0

# CLI enhancements (optional but recommended)
colorama>=0.4.6

# Note: Heavy dependencies like scapy and aioquic are NOT required
# for basic operation. They can be installed manually if needed:
# pip install scapy>=2.5.0 aioquic>=0.9.21
